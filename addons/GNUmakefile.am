OPTDIRS =

if CONFIG_RTAI_CPLUSPLUS
OPTDIRS += cpp
endif

if CONFIG_RTAI_COMEDI_LXRT
OPTDIRS += comedi
endif

if CONFIG_RTAI_SERIAL
OPTDIRS += serial
endif

if CONFIG_RTAI_FUSION
OPTDIRS += fusion
endif

SUBDIRS = $(OPTDIRS)
