EXTRA_CFLAGS += -I$(fusion_srctree)/include \
		-I$(fusion_srcdir)/.. \
		-I$(src)/../../include \
		-I$(src)/../.. \
		$(fusion_extradef)

obj-m += $(rtai_target).o

$(rtai_target)-objs := $(fusion_objs)
