testdir = $(prefix)/testsuite/cruncher

test_PROGRAMS = cruncher

cruncher_SOURCES = cruncher.c

cruncher_CPPFLAGS = \
	-I$(top_srcdir)/include \
	-I$(top_srcdir)/skins \
	-I../../include

cruncher_CFLAGS = \
	@RTAI_USER_CFLAGS@ \
	@RTAI_ARCH_TUNE_FLAGS@ \
	-funroll-loops

cruncher_LDADD = \
	../../nucleus/lib/libfusion.la \
	-lpthread

install-data-local:
	$(mkinstalldirs) $(DESTDIR)$(testdir)
	$(INSTALL_DATA) $(srcdir)/runinfo $(DESTDIR)$(testdir)/.runinfo
	@echo "\$${DESTDIR}$(prefix)/bin/rtai-load \$$*" > $(DESTDIR)$(testdir)/run
	@chmod +x $(DESTDIR)$(testdir)/run

uninstall-local:
	$(RM) $(DESTDIR)$(testdir)/.runinfo $(DESTDIR)$(testdir)/run

run: all
	@$(top_srcdir)/scripts/rtai-load --verbose

EXTRA_DIST = runinfo
