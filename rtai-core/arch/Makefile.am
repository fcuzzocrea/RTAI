if CONFIG_X86
ARCHDIR = i386
endif

if CONFIG_ARM
ARCHDIR = arm
endif

if CONFIG_PPC
ARCHDIR = ppc
endif

if CONFIG_MIPS
ARCHDIR = mips
endif

if CONFIG_CRIS
ARCHDIR = cris
endif

SUBDIRS = $(ARCHDIR)
